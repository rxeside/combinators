import { BridgeMessage } from "../../core/bridge";
import { Widget, WidgetEvents } from "../../core/widget";
import { FloatingOneTapInternalEvents } from './events';
import { FloatingOneTapParams } from './types';
export declare class FloatingOneTap extends Widget<Omit<FloatingOneTapParams, 'appName'>> {
    protected vkidAppName: string;
    protected onBridgeMessageHandler(event: BridgeMessage<FloatingOneTapInternalEvents | WidgetEvents>): void;
    private createAgreementsDialogWidget;
    private openFullAuth;
    private renderOAuthList;
    render(params: FloatingOneTapParams): this;
}
