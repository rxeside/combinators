import { BridgeMessage } from "../../core/bridge";
import { Widget, WidgetEvents } from "../../core/widget";
import { AgreementsDialogInternalEvents } from './events';
export declare class AgreementsDialog extends Widget {
    protected vkidAppName: string;
    protected templateRenderer: (id: string) => string;
    protected onBridgeMessageHandler(event: BridgeMessage<AgreementsDialogInternalEvents | WidgetEvents>): void;
}
