import { WidgetParams } from "../../core/widget";
import { OAuthListParams, OAuthName } from "../oauthList";
import { OneTapParams, OneTapStyles } from './types';
type OneTapTemplateParams = Required<OneTapStyles> & Pick<OneTapParams, 'skin'> & Pick<WidgetParams, 'scheme' | 'lang'> & {
    openFullAuth?: void;
    iframeHeight?: number;
    renderOAuthList: (params: OAuthListParams) => void;
    providers?: OAuthName[];
};
export declare const getOneTapTemplate: ({ width, height, iframeHeight, borderRadius, openFullAuth, skin, scheme, lang, renderOAuthList, providers, }: OneTapTemplateParams) => (id: string) => string;
export {};
