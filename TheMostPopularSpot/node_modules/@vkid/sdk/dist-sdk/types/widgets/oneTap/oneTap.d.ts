import { BridgeMessage } from "../../core/bridge";
import { Widget, WidgetEvents } from "../../core/widget";
import { OneTapInternalEvents } from './events';
import { OneTapParams } from './types';
export declare class OneTap extends Widget<OneTapParams> {
    protected vkidAppName: string;
    protected onBridgeMessageHandler(event: BridgeMessage<OneTapInternalEvents | WidgetEvents>): void;
    private createAgreementsDialogWidget;
    private openFullAuth;
    private renderOAuthList;
    render(params: OneTapParams): this;
}
